import { StyleSheet, Text, View } from "react-native";

const Order = () => {
  return (
    <View>
      <Text>Orders</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Order;
